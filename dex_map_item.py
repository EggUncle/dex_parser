#!/usr/bin/python
# -*- coding: UTF-8 -*-

class Dex_map_item(object):
    def __init__(self, type, unused, size, offset):
        self.type = type
        self.unused = unused
        self.size = size
        self.offset = offset

